# Living World Engine - 면접 예상 질문 & 답변

> 개발하면서 나올 수 있는 면접 질문을 정리하고, 실제 경험 기반으로 답변을 준비하는 문서
> 카테고리: 설계, LLM, 테스트, 문제 해결, 성장

---

## 1. 설계 & 아키텍처

### Q. 이 프로젝트를 한 문장으로 설명해주세요.
> Character.AI 같은 챗봇의 한계(기억 부족, 반복 루프, 상태 변화 없음)를 해결하는
> AI 기반 대화형 RPG 엔진입니다. Claude API의 Tool Use를 활용해서
> LLM이 직접 게임 상태를 제안하고, 시스템이 이를 검증하는 구조입니다.

### Q. 왜 LangChain을 안 썼나요?
> 불필요한 추상화라고 판단했습니다.
> LangChain을 쓰면 내부에서 무슨 일이 일어나는지 블랙박스가 되어
> 면접에서 "이게 왜 이렇게 동작해요?"라는 질문에 답하기 어렵습니다.
> Anthropic API를 직접 사용함으로써 Tool Use의 2단계 호출 과정을
> 완전히 이해하고 제어할 수 있었습니다.

### Q. 기존 버전(ai_s)이 있었는데 왜 새로 만들었나요?
> ai_s는 3-Layer 아키텍처에 FSM, Rule Engine, StateReducer 등
> 시스템이 너무 많아서 디버깅이 사실상 불가능했습니다.
> 특히 StateReducer에서 여러 소스의 상태 변경이 충돌하는 문제가 있었고,
> NPC 행동이 항상 "challenge"로 고착되는 버그를 FSM 구조 안에서는 해결할 수 없었습니다.
>
> 그래서 핵심 원칙을 세웠습니다:
> "LLM에게 맡길 건 맡기고, 시스템은 검증만 확실히 하자."
> 복잡한 FSM 대신 LLM이 맥락을 보고 직접 판단하게 하고,
> Validator가 변화량 제한과 캐릭터 존재 확인만 하는 방식으로 바꿨습니다.

### Q. 상태 관리를 어떻게 설계했나요?
> WorldState 클래스가 모든 게임 상태를 관리합니다.
> NPC 관계는 0-100 범위로 클램핑되고,
> LLM이 제안한 변경은 반드시 Validator를 거쳐서
> 한 턴에 -10~+10 범위로 제한됩니다.
> 이렇게 하면 LLM이 아무리 극단적인 값을 제안해도
> 게임 밸런스가 깨지지 않습니다.

---

## 2. LLM & Tool Use

### Q. Claude의 Tool Use를 어떻게 사용했나요?
> 게임 턴마다 2단계 호출을 합니다.
>
> 1단계: 플레이어 입력 + 게임 상태를 Claude에게 보내면,
> Claude가 `update_game_state` 도구를 호출하며
> 관계 변화와 새 기억을 JSON으로 제안합니다.
>
> 2단계: 시스템이 도구 실행 결과(tool_result)를 돌려보내면,
> Claude가 최종 서사 텍스트를 생성합니다.
>
> 이렇게 하면 상태 변경과 서사가 분리되어
> 각각 독립적으로 검증할 수 있습니다.

### Q. Tool Use에서 LLM이 잘못된 값을 보내면 어떻게 하나요?
> StateChangeValidator가 3단계로 검증합니다.
>
> 1. 캐릭터 존재 확인 — 없는 NPC 이름이면 거부
> 2. 스탯 유효성 — affection/trust/respect/fear 외에는 거부
> 3. 변화량 클램핑 — ±10 초과분은 ±10으로 잘라냄
>
> 기억도 검증합니다: 빈 내용 거부, 중요도 1-10 범위 제한,
> 유효하지 않은 감정 태그는 "neutral"로 대체.
> 이 모든 검증 로직에 대해 유닛 테스트 9개가 커버합니다.

### Q. GPT 대신 Claude를 선택한 이유는?
> 세 가지입니다.
>
> 1. Tool Use 품질: Claude가 스키마를 더 잘 따릅니다.
>    GPT는 가끔 스키마와 다른 필드를 추가하거나 타입을 틀리는 경우가 있었는데,
>    Claude는 정의한 enum 값을 정확히 사용합니다.
>
> 2. 차별화: 포트폴리오에서 "GPT만 써봤다"는 흔하지만,
>    "Claude API를 직접 통합했다"는 경험이 더 눈에 띕니다.
>
> 3. 비용 대비 품질: Sonnet 4.5는 GPT-4o-mini와 비슷한 가격대인데,
>    한국어 이해도와 Tool Use 안정성이 더 좋았습니다.

---

## 3. 테스트 & 품질

### Q. LLM 코드를 어떻게 테스트하나요?
> pytest-mock으로 Anthropic API를 Mock합니다.
> 가짜 응답 객체(FakeTextBlock, FakeToolUseBlock, FakeResponse)를 만들어서
> 실제 API를 호출하지 않고도 다음을 검증합니다:
>
> - Tool Use 시 API가 정확히 2번 호출되는지
> - 2차 호출의 messages에 tool_result가 포함되는지
> - state_changes가 정확하게 추출되는지
> - API 에러 시 예외가 올바르게 전파되는지
>
> 총 15개 Mock 테스트로 API 비용 없이 LLM 통합 로직을 검증합니다.

### Q. 테스트 커버리지는 어떻게 되나요?
> 현재 53개 유닛 테스트가 100% 통과합니다.
> 핵심 모듈별로:
> - state.py: 14개 (관계 클램핑, NPC 조회, 상태 적용)
> - memory.py: 9개 (키워드 매칭, 중요도 정렬, 최신도)
> - validator.py: 9개 (범위 제한, 캐릭터 검증, 감정 태그)
> - loop_detector.py: 6개 (정체 감지, 반복 감지)
> - llm.py: 15개 (Mock Tool Use 2단계, 에러 처리)

---

## 4. 문제 해결

### Q. 개발 중 가장 어려웠던 문제는?
> (개발 진행하면서 추가할 것)
>
> 예시 포맷:
> **문제**: [구체적 상황]
> **원인 분석**: [왜 발생했는지]
> **시도한 방법**: [어떤 접근을 했는지]
> **최종 해결**: [어떻게 해결했는지]
> **배운 점**: [이 경험에서 얻은 것]

### Q. 기존 버전에서 가장 심각했던 버그는?
> NPC intent "challenge" 고착 문제입니다.
>
> ai_s에서 NPCBehaviorEngine의 FSM이 매 턴 NPC의 intent를 계산했는데,
> 플레이어가 선물을 줘도, 대화를 해도 항상 "challenge"로 돌아갔습니다.
> 원인은 FSM의 상태 전이 조건에 플레이어 행동이 반영되지 않았기 때문이고,
> FSM 구조 안에서 이걸 고치려면 상태 전이 테이블 전체를 재설계해야 했습니다.
>
> 그래서 관점을 바꿨습니다.
> "NPC 행동을 규칙으로 정의하는 게 아니라,
> LLM이 대화 맥락과 관계 수치를 보고 직접 판단하게 하자."
> FSM을 아예 제거하고 LLM에게 위임한 결과,
> 플레이어 행동에 자연스럽게 반응하는 NPC가 만들어졌습니다.

### Q. 루프 감지는 어떻게 구현했나요?
> 두 가지 방식을 결합했습니다.
>
> 1. 상태 정체 감지: 최근 5턴의 관계 수치 변화량을 합산하고,
>    임계값(0.05) 미만이면 "정체"로 판정합니다.
>
> 2. 대사 반복 감지: 현재 응답과 이전 응답들의 Jaccard 유사도를 계산하고,
>    0.8 이상이면 "반복"으로 판정합니다.
>
> 루프가 감지되면 EventManager가 강제 이벤트를 주입해서
> 서사를 새로운 방향으로 전환합니다.

---

## 5. 성장 & 회고

### Q. 이 프로젝트를 통해 성장한 점은?
> (매주 업데이트할 것)
>
> **Week 1:**
> - Claude Tool Use의 2단계 호출 패턴을 직접 구현하면서
>   LLM API의 동작 원리를 깊이 이해하게 되었습니다.
> - 기존 코드(ai_s)를 분석하고 "무엇을 버릴지"를 결정하는 것이
>   "무엇을 만들지" 결정하는 것만큼 중요하다는 걸 배웠습니다.
> - Mock 테스트로 외부 API 의존 없이 로직을 검증하는 방법을 익혔습니다.

### Q. 다시 만든다면 뭘 바꾸겠나요?
> (프로젝트 완료 후 작성)

### Q. 향후 개선 계획은?
> (프로젝트 완료 후 작성)

---

## 6. 기술 심화 질문

### Q. 왜 벡터 DB 대신 키워드 매칭을 사용했나요?
> 이 프로젝트의 메모리 규모에서는 키워드 매칭이 더 적합합니다.
>
> 벡터 DB(ChromaDB 등)는 수만~수십만 건의 문서를 검색할 때 효과적이지만,
> 게임 세션 하나에 축적되는 기억은 수십~수백 건 수준입니다.
> 이 규모에서 임베딩 생성 + 벡터 검색은 오버헤드만 큽니다.
>
> 대신 키워드 매칭(50%) + 중요도(30%) + 최신도(20%)로
> 가중치를 결합하는 방식을 택했습니다.
> 불용어 제거로 노이즈를 줄이고, 시간 감쇠 함수로 최신 기억에 가중치를 줍니다.
> 충분히 효과적이면서 외부 의존성이 제로입니다.

### Q. 메모리 검색의 시간 복잡도는?
> O(N * K)입니다. N은 총 기억 수, K는 쿼리 키워드 수.
> N이 수백 건 수준이므로 지연 없이 동작합니다.
> 만약 N이 수천 건을 넘긴다면, 역인덱스를 도입하거나
> 중요도 하위 기억을 주기적으로 정리하는 방식으로 대응할 수 있습니다.

### Q. 동시성 문제는 없나요? (멀티 세션)
> (Week 3 API 구현 후 작성)

### Q. 프롬프트 엔지니어링은 어떻게 했나요?
> (Day 2~ 프롬프트 튜닝 후 작성)

---

## 추가할 질문 메모

개발하면서 새로운 질문이 떠오르면 여기에 메모:

- [ ] (예시) 세이브/로드 시 데이터 일관성은 어떻게 보장하나요?
- [ ] (예시) 프론트엔드와 백엔드 간 실시간 통신은 어떻게 하나요?
